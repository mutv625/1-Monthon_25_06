using UnityEngine;

public class PlayerAnimator : MonoBehaviour
{
    // 初期化はエントリーポイントで行う??
    private Animator animator;

    void SetAnimation(SerDictionary<string, AnimationClip> animation)
    {
        
    }

}
